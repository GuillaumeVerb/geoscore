# GeoScore — environment files live per app (Next.js does not load this root file).
#
# Backend:  copy backend/.env.example → backend/.env
# Frontend: copy frontend/.env.example → frontend/.env.local
#
# See README.md and docs/03-build/verify-deployment.md for run and deploy checks.
